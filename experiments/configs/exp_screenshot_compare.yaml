name: "exp_screenshot_compare_v1"
seed: 42
max_steps_per_episode: 40
runs_per_task: 1

variants:
  - screenshot_based
  - state_aware
  - typed_action
  - typed_action_priors

task_sets:
  - starter_20

ui_perturbation_profile: "default"
